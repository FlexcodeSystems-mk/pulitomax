kind = "web"
previewPath = "/"
title = "PulitoMax — Cleaning Services Italy"
version = "1.0.0"
id = "artifacts/cleaning"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23422

[services.development]
run = "pnpm --filter @workspace/cleaning run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/cleaning", "run", "build" ]
publicDir = "artifacts/cleaning/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23422"
BASE_PATH = "/"
